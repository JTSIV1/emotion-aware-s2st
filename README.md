# Emotion Aware S2ST

## Environment Setup

```bash
python -m venv venv

source venv/bin/activate # or venv/Scripts/activate on windows

pip install -r requirements.txt
```

## Load Dataset

Simply run the `load_data.py` file and it will download and extract the dataset to a ./data directory

```bash
python load_data.py
```